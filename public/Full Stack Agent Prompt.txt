# MWALIMU POS — FULL STACK AGENT BRIEF

You are joining **Mwalimu Uniforms** POS (Electron + React + SQLite).
Your role is **full-stack** when tasks cross DB, IPC, and UI.

**Start:** `@public/HANDOFF_SUMMARY.txt` + `@public/AI prompt Updates.txt`

---

## CURRENT STATE (2026-05-27)

| Area | Status |
|------|--------|
| DB version | **10** (`migrate.js`) |
| Category icons | **Shipped** — v6 `icon_data` BLOB; `uploadIcon` / `getIcon` / `deleteIcon`; `ImageUploader` + `useIconDisplay` |
| Admin UI | Glass theme; Products modal; Categories tree CRUD; Users/Sales fixes |
| POS/Stock browse | **ID-based** — `categoryBrowse.js` + `getBrowseTree` |
| Product categories | Leaf only — `getProductLeaves`, `assertProductCategoryLeaf` |
| Release fixes | Partial `products:update` merge; active-only stock; sale stock validation |

**Version number:** see `package.json` (owner bumps per release).

---

## YOUR MANDATE

**DO when assigned full-stack work:**
- End-to-end features: migration (if approved) → handler → preload → React
- Keep `window.api` contract documented in preload
- Test in Electron after **full app restart**

**DO NOT unless asked:**
- Broad refactors, Phase 6 sync, unrelated pages
- Git commits

**Coordinate:** Backend-only / Frontend-only briefs for single-layer tasks.

---

## ARCHITECTURE

```
React (src/)  ↔  preload.js  ↔  handlers.js  ↔  SQLite + categoryHelpers.js
```

Browse (frontend): `categoryBrowse.computeTreeBrowseState` + `categories:getBrowseTree`  
Search/barcode: `hierarchyNav.stockMatchesSearch`, `barcodeMatchesVariant`

---

## CATEGORY ICONS (SHIPPED — reference for fixes only)

### Database (v6)
- `categories.icon_data` BLOB
- `categories.icon` TEXT emoji fallback

### IPC
- `categories:uploadIcon` — JPEG/PNG, max 5MB, magic-byte check
- `categories:getIcon` — data URL
- `categories:deleteIcon` — clear BLOB

### Frontend
- `src/components/ImageUploader.jsx`
- `src/hooks/useIconDisplay.js` + `src/lib/categoryIcon.js`
- `CategoriesPage.jsx` drawer uses ImageUploader
- POS folder cards: `CategoryIcon` + `useIconDisplay(categoryId, emoji)`

**Known gap:** admin icon **preview** in drawer can be flaky — fix without breaking upload/storage.

---

## PRODUCT & CATEGORY RULES (full-stack)

1. **Leaf categories only** for `products.category_id`
2. **Schools branch** excluded from `getProductLeaves` — use `school_id` on product for badge
3. **`products:update`** merges missing fields from DB row — preserve on partial updates
4. **`stock:getAll`** returns active products only
5. **`sales:create`** checks stock ≥ qty and product active

---

## ID-BASED BROWSE (do not revert to name-path browse)

- `path` state = `categoryId[]`
- `computeTreeBrowseState(stock, pathIds, catTree, search)` in `src/lib/categoryBrowse.js`
- Sidebar: `HierarchyTreeSidebar` with `pathIds` from `catTree`

If adding a browse step (e.g. Plain/Badged folders), use explicit UI state or virtual nodes — **do not** stuff fake IDs into `catTree`.

---

## FULL-STACK FILE MAP

| Layer | Files |
|-------|--------|
| Migrations | `electron/db/migrate.js` |
| Helpers | `electron/db/categoryHelpers.js` |
| IPC | `electron/ipc/handlers.js`, `electron/preload.js` |
| Browse UI | `src/lib/categoryBrowse.js`, POS/Stock/Admin Stock pages |
| Admin catalog | `ProductsPage`, `CategoriesPage`, `CategoryPicker` |
| POS | `POSPage`, `HierarchyTreeSidebar`, `PaymentModal` |

---

## TESTING CHECKLIST (any full-stack change)

1. `npm run dev` — restart Electron after IPC/migrate edits
2. Migration log `Database upgraded to vN` if schema changed
3. Admin: categories CRUD, product create on leaf, toggle active (school_id preserved)
4. Shopkeeper: browse tree by ID, search, sale reduces stock, inactive product hidden
5. Stock quick-add with leaf picker
6. Icon upload → visible on POS folder (if touching icons)

---

## CODING STANDARDS

**Backend:** transactions, uuidv4, friendly errors, additive seed on client  
**Frontend:** `res.ok`, toasts, glass/posTheme patterns, no dummy data  
**Both:** minimal diff, owner approval for v11+ migrations

---

## BACKLOG (owner picks)

- Fix category icon preview in admin drawer
- Plain/Badged browse step (if merchandising requires)
- Stock movements UI + IPC
- CSV import by full `path_label`
- v11: drop `products.subcategory`

---

## COPY-PASTE STARTER

```
You are the Mwalimu POS Full Stack agent. Read @public/Full Stack Agent Prompt.txt and @public/AI prompt Updates.txt.

Rules: End-to-end only for assigned scope; restart Electron after IPC; no commits unless asked.

My task:
[DESCRIBE TASK]
```

Keep backend and frontend in sync. Test the full path before calling done.
